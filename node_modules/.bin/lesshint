#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../lesshint-antd/bin/lesshint" "$@"
  ret=$?
else 
  node  "$basedir/../lesshint-antd/bin/lesshint" "$@"
  ret=$?
fi
exit $ret
